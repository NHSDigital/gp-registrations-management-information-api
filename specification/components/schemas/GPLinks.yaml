type: object
required:
  - gpLinksComplete
properties:
  gpLinksComplete:
    type: boolean
    description: |
      **true** - If the required GP Links information has been completed for this registration
      **false** - If the required GP Links information has not been completed for this registration
    example: true