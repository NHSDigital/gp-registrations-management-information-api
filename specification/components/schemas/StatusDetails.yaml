type: object
required:
  - status
properties:
  status:
    type: string
    description: The status
    example: FAILURE
    enum:
      - SUCCESS
      - FAILURE
  reason:
    type: string
    description: The reason for the status
    example: Could not retrieve record